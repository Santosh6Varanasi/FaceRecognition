/**
 * Angular Production Environment Configuration
 * 
 * This file is used when building for production with:
 * ng build --configuration production
 * 
 * Update the apiBaseUrl to point to your production Next.js backend URL.
 */

export const environment = {
  production: true,
  // Update this to your production Next.js backend URL
  apiBaseUrl: 'https://api.yourdomain.com'
};
